syntax = "proto3";

package pb;

service StreamService{
    rpc Stream (stream Envelope) returns (stream Envelope) {}
}

message Envelope {

    // marshalled Message
    bytes payload = 1;

    // signed Message
    bytes signature = 2;

    // sender's public key
    bytes pubkey = 3;

    string protocol = 4;
}